# Development dependencies
# Install qualitybase, providerkit and geoaddress in editable mode for local development
-e ../python-qualitybase

# Include production dependencies
Django>=3.2
pytest>=7.0
pytest-django>=4.5
ruff>=0.5
mypy>=1.0
django-stubs>=5.0.0
python-dotenv>=1.0
